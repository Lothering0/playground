{
  "name": "ts-project-template",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "node ./esbuild.config.mjs",
    "tsc": "tsc -p ./tsconfig.json",
    "tsc:watch": "tsc -p ./tsconfig.json --watch",
    "watch": "nodemon dist/index.js",
    "nodemon": "nodemon",
    "tsc:nodemon:watch": "tsc --watch index.ts && nodemon index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "reflect-metadata": "^0.2.2"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "esbuild": "^0.25.1",
    "nodemon": "^3.0.2",
    "typescript": "^5.3.3"
  }
}
